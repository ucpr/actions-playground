name: environment

on:
  push:
    tags:
      - 'v*'

jobs:
  environment:
    environment:
      name: approve_test

    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3.0.0
      - run: echo 'Hello'
